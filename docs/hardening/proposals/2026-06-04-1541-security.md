# Hardening Proposal — security — 2026-06-04

Baseline score: S₀ = 62/100   (open: C1 H0 M3 L1)
Target: 90   → **reached: S₁ = 99/100 (Δ +37)**

## Red team found
- **[CRITICAL]** Path traversal @ `src/app/api/local-mdx/route.ts:17` — the user-controlled
  `file` query param flowed straight into `path.join(cwd,'src','app','local-mdx',file)` +
  `readFileSync` with no sanitization. `?file=../../../.env` reads the project's own secrets.
  **Chains** with the env-prefix finding below (traversal is the vector that exfiltrates them).
- **[MEDIUM]** Public-prefixed secret @ `.env` — `NEXT_PUBLIC_PINATA_API_KEY` /
  `NEXT_PUBLIC_PINATA_SECRET_API_KEY` carry the client-bundle prefix. Verified latent: not
  referenced in `src/`, absent from `.next/static`, no client use in git history → not a live
  leak, but a footgun.
- **[MEDIUM]** XSS sink @ `SmartContractLegalQuiz.tsx:93` — `dangerouslySetInnerHTML` on
  `quiz.question`. Data is static/author-authored (THEORETICAL), but the sink is removable.
- **[MEDIUM]** No security headers @ `next.config.ts` — no `X-Content-Type-Options`,
  `X-Frame-Options`, `Referrer-Policy`, `HSTS`, `Permissions-Policy`, or CSP.
- **[LOW]** Untrusted remote-MDX `evaluate()` @ `ClientMdxLoader.tsx` — dead code (no importer).

## Blue team — applied this cycle
- **Fix #2** (+25): filename allowlist `^[a-zA-Z0-9_-]+\.mdx$` + `path.resolve` containment
  check before read. Risk low — all 14 post slugs + `sample.mdx` pass the regex.
- **Fix #1** (+4): dropped `NEXT_PUBLIC_` prefix → `PINATA_API_KEY` / `PINATA_SECRET_API_KEY`
  (server-only). Rotation **not** required (git history shows no past client reference).
- **Fix #11** (+4): render `quiz.question` as text with `whitespace-pre-line`; converted
  `<br />` → `\n`. XSS sink removed permanently.
- **Fix #12** (+4): added `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`,
  `Permissions-Policy`, `Strict-Transport-Security` for all routes.
- **Fix #13** (+1): deleted dead `ClientMdxLoader.tsx` + `integrity.ts`.

Verification: `tsc --noEmit` PASS, `next build` PASS (Node 22; 12 routes compiled).

## Out of scope this cycle (residual / follow-ups)
- **CSP (Content-Security-Policy)** — LOW, deferred. Needs runtime testing against AdSense,
  Pinata IPFS gateway, Infura RPC, and S3 images before enforcing; a wrong CSP would break the
  live site. This is the single open LOW keeping the score at 99 rather than 100.
- **Vercel env vars** — the `.env` rename is local; the deployment platform's env var names must
  be updated there too (separate from the repo).
- Build requires Node ≥ 18.18 (project was on 18.16 locally; verified on Node 22).
